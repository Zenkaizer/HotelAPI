spring:
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3306/db_codecrafters
    username: root
    password: mcnspede1_
  sql:
    init:
      platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 9000